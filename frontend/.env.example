# API gateway base URL (Kong)
# Keep this pointed at Kong so UI never calls atomic/composite services directly.
VITE_API_BASE_URL=http://localhost:8000

# Realtime websocket via Kong gateway
VITE_REALTIME_WS_URL=ws://localhost:8000/ws
