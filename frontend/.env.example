# Optional: direct API base URL (useful in production deployments)
# If omitted, the app uses /api/passenger (proxied by Vite dev server).
VITE_PASSENGER_API_BASE=https://personal-4whagfbm.outsystemscloud.com/Passenger_Srv/rest/PassengerAPI
